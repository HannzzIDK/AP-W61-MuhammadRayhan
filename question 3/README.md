3.the main output if the program is "Programmer writes code" and "Employee attends meeting" well, the question wants me to tell why does one method use the superclass version whereas the 2nd one use a subclass version, logically the Programmer class is a subclass of Employee class which has the method of work, the programmer class explicitly overrides this class by changing the output from "employee is working" to "Programmer writes code", the 2nd method which is meeting is not overriden in any way so, any objects created can use the meeting method directly form the employee class. in the main method,a Programmer object was created and 2 methods are called which are work and meeting. the work used the overriden method from the programmer class whereas the meeting is directly called from the employee class
