2. the two lines int the main class (as the question states) has a completely different output, in the main class 2 objects were created which are Vehicle and Car, both contains the same move method but both objects execute it completely differently, the Vehicle object derives from the vehicle class which has the output of "Vehicle is moving" its different form the Car class "Car is moving" (the move method from that class is overridden and given  new instructions). a simple example of polymorphism